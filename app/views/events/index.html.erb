<p></p>
<%= link_to '新增事件', new_event_path, :class => "button", :style => "color: white" %>
<hr/>
<p style="font-size:20px">所有事件</p>
<table class="table talbe-bordered">
  <thead>
    <tr>
      <th style="width:200px;">事件</th>
      <th style="width:200px;">待辦事項數量</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr class="admin">
        <td><%=link_to event.title, event_path(event) %></td>
        <td><%= event.tasks.count %></td>
        <td><%= link_to '刪除', event, method: :delete, data: { confirm: '確定嗎？' }, :class => "delete" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
